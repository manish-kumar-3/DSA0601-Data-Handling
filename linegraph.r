# Sample data
x_values <- c(1, 2, 3, 4, 5)
y_values <- c(10, 15, 7, 12, 9)

# Plotting the line graph
plot(x_values, y_values, type='l', col='blue', lwd=2, pch=16, xlab='X-axis Label', ylab='Y-axis Label', main='Line Graph ')

# Adding grid
grid()
